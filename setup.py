from setuptools import setup, find_packages

setup(
    name='Employee Management system',
    version='1.0.0',
    description='This pacakge contains code for an employee management system',
    author='Chanel Johnson',
    url='https://github.com/Chanel456/employee_management_system',
    packages=find_packages(exclude=('tests'))
)